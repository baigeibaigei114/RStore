<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.remotesensing.platform.mapper.AiPlanMapper">

    <sql id="AiPlanColumnList">
        id,
        user_id,
        user_input,
        title,
        status,
        plan_json::text AS plan_json,
        validation_errors::text AS validation_errors,
        created_at,
        updated_at
    </sql>

    <insert id="insert" parameterType="com.remotesensing.platform.entity.AiPlan"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_plan (
            user_id,
            user_input,
            title,
            status,
            plan_json,
            validation_errors
        ) VALUES (
            #{userId},
            #{userInput},
            #{title},
            #{status},
            CAST(#{planJson} AS jsonb),
            CAST(#{validationErrors} AS jsonb)
        )
    </insert>

    <select id="selectByIdForUser" resultType="com.remotesensing.platform.entity.AiPlan">
        SELECT
        <include refid="AiPlanColumnList"/>
        FROM ai_plan
        WHERE id = #{id}
          AND user_id = #{userId}
    </select>

    <select id="pageByUser" resultType="com.remotesensing.platform.entity.AiPlan">
        SELECT
        <include refid="AiPlanColumnList"/>
        FROM ai_plan
        WHERE user_id = #{userId}
        <if test="query != null and query.status != null and query.status != ''">
          AND status = #{query.status}
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUser" resultType="long">
        SELECT COUNT(1)
        FROM ai_plan
        WHERE user_id = #{userId}
        <if test="query != null and query.status != null and query.status != ''">
          AND status = #{query.status}
        </if>
    </select>

    <update id="updateStatusForUser">
        UPDATE ai_plan
        SET status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>
</mapper>
